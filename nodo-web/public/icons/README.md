# Icons

Placeholder PNGs (192, 512, 512-maskable). Generar con `scripts/generate-icons.js` del plan app-web o reemplazar con branding real antes de deploy.

Referenciados desde:
- `index.html` — favicon + apple-touch-icon
- `vite.config.ts` — manifest PWA
